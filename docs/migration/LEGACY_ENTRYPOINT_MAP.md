# Legacy entrypoint map

Machine-readable mapping: `configs/legacy_entrypoint_map.json`.

Core wrappers retain command/import compatibility while delegating to the
package. The `scripts/legacy/` paths are archived historical entrypoints and
are not replacements for future formal CLIs.
