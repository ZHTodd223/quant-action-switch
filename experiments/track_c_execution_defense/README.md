# Track C: execution and defense

Status: `DRAFT_REQUIRES_USER_APPROVAL`; `gpu_execution_ready=false`.
Only the deterministic synthetic executor is in scope. It has no filesystem,
network, shell, credential, account, email, or payment capability.
