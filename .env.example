# Never commit real values. Export these in the server shell or secret manager.
HF_TOKEN=
MODELSCOPE_TOKEN=

# Optional: only needed for private GitHub cloning on the server.
GH_TOKEN=

# Local paths on the ephemeral server.
PROJECT_ROOT=/mnt/workspace/quant-action-switch
NAS_ROOT=/mnt/data/quant-action-switch
CACHE_ROOT=/mnt/data/quant-action-switch/cache
MODEL_DIR=/mnt/data/quant-action-switch/cache/models/Qwen2.5-1.5B-Instruct

# Alibaba Cloud mirror is close to the selected Hangzhou instance.
PIP_INDEX_URL=https://mirrors.aliyun.com/pypi/simple/
